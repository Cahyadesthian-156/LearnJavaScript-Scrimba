//create person object contain three keys : name, age, and country
let person = {
    name: "Cahyadesthian Widigda",
    age : 21,
    country : "Indonesia"
}

//create function logData() ti log out followinf format
function logData() {
    console.log(person.name + " is " + person.age + " years old and lives in " + person.country);
}

// "Per is 35 Years Old and lives in Norway"
//cal the logData() function to verifiy that function is work
logData()
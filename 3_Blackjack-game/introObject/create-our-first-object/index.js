//create object taht represent airbnb caste listing
//it should contain at leat one boolean, one string, one numberm adn one array
//log at least two of the keys using dot notation

let place = {
    available : true,
    title : "House of the week",
    build : 4,
    facilities : ["bedroom", "dining room", "kitchen"] 
}

console.log("Place name: " + place.title + "\nAvailable: " + place.available);